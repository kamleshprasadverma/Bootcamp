import java.util.*;
import java.io.*;
class rev
{
	public static void main (String args[])
	{
		string input ="hellow":
		char[] temp=input.to CharArray();
		int left,right=0;
		rigth=temp.lenght-1;
			for(left=0;left<right;left++,right--)
			{
				char tem=temp[left]
				temp[left]=temp[rigth]
				temp[rigth]=tem;
			}
			for(char c:temp)
			S ystem.out.print(c);
			System.out.println();
	}
}